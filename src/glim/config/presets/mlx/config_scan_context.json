{
  "scan_context": {
    "lidar_height": 0.82,
    "pc_num_ring": 20,
    "pc_num_sector": 60,
    "pc_max_radius": 30.0,
    "pc_fov": 120.0,
    "pc_fov_offset": -60.0,
    // Reduced exclusion for more loop detection opportunities
    "num_exclude_recent": 30,
    "num_candidates_from_tree": 10,
    "search_ratio": 0.2,
    // Relaxed matching threshold (0.3 instead of 0.2)
    "sc_dist_threshold": 0.30,
    "tree_making_period": 30,
    "min_movement_threshold": 1.5,
    // Relaxed overlap requirement for indoor parking lot
    "inlier_fraction_threshold": 0.65,
    // Increase ICP iterations for better convergence
    "icp_max_iterations": 50,
    "icp_num_threads": 4,
    // VERY Strong loop constraint for maximum accuracy (lower = stronger)
    "loop_noise_score": 0.05
  }
}