{
  /*** RGB Colorizer Configuration ***/
  "rgb_colorizer": {
    // Path to LiDAR-Camera calibration file (JSON format)
    // The calibration file should contain:
    // - camera_matrix: fx, fy, cx, cy, skew
    // - dist_coeffs: k1, k2, k3, p1, p2
    // - quaternions: x, y, z, w (LiDAR to Camera rotation)
    // - tvecs: tx, ty, tz (LiDAR to Camera translation)
    "calibration_file": "/home/soslab/Documents/bags/Pangyo_indoor_parking_lot_slam_2025_11_19-19_31_37/calibration_params.json",

    // Time sync tolerance between image and point cloud (seconds)
    "sync_tolerance": 0.1,

    // Maximum number of images to buffer
    "max_buffer_size": 30
  }
}
